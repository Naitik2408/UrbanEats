# Environment Configuration for UrbanEats Backend
# Copy this file to .env and update with your actual values
# DO NOT commit .env file to version control

# Spring Profile (local, prod)
SPRING_PROFILE=local

# Server Configuration
SERVER_PORT=8080

# JWT Configuration
# CRITICAL: JWT secret must be at least 32 characters (256 bits) for HS256 algorithm
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET=urbanEatsSecretKeyForJWTTokenGenerationMinimum256BitsRequired
JWT_EXPIRATION=86400000

# OTP Configuration
OTP_EXPIRATION=120
OTP_LENGTH=6
OTP_MAX_ATTEMPTS=5
OTP_COOLDOWN=30
OTP_RETURN_IN_RESPONSE=true

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=urbaneats
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_POOL_SIZE=10
DB_MIN_IDLE=5
DB_CONNECTION_TIMEOUT=30000
DB_IDLE_TIMEOUT=600000
DB_MAX_LIFETIME=1800000

# JPA Configuration
JPA_DDL_AUTO=update

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_TIMEOUT=60000
REDIS_POOL_MAX_ACTIVE=8
REDIS_POOL_MAX_IDLE=8
REDIS_POOL_MIN_IDLE=0

# Elasticsearch Configuration
ELASTICSEARCH_URIS=http://localhost:9200
ELASTICSEARCH_USERNAME=
ELASTICSEARCH_PASSWORD=
ELASTICSEARCH_CONNECTION_TIMEOUT=5s
ELASTICSEARCH_SOCKET_TIMEOUT=30s

# Logging Configuration
LOG_LEVEL_ROOT=INFO
LOG_LEVEL_APP=DEBUG
LOG_LEVEL_SPRING_WEB=INFO
LOG_LEVEL_SECURITY=INFO
LOG_LEVEL_HIBERNATE=WARN
